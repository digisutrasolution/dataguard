{
  "name": "dataguard",
  "version": "0.1.0",
  "private": true,
  "description": "DataGuard Solutions — run backend + frontend together",
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "setup": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "migrate": "npm --prefix backend run migrate",
    "seed": "npm --prefix backend run seed",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
